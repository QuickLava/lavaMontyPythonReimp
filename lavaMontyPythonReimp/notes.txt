cf48

sword glow length = a0?
2f520

diff = 0x84 or 132
0x20D00

Plan:
Moveset Patches
- Specify a list of functions to target and which parameter to target
- a list of modifications to apply to elements which match some pattern
- for now, just implement replacement

fit marth.pac
attributes start at 0x0 relative to moveset start
looks like 2e4*2 bytes long, first half is normal attrs, second is sse attrs

common action flags start right after that
there's a block of flags that'd I guess be all FF
not sure how far that extends exactly

next attested thing in psac is extra action flags @ 1768



next after that is dataflags 2 @ 73DF


anim names @ 1f3cc, reffed as 1F34C
this is found at the end of the name section
interestingly, this seems to be part of a table
every offset in the table is followed/preceded by a num
not sure what these are yet, but it probably isn't a count or offset. Individual bytes seem to signifiy things, 
I've seen:
Byte0: 00, 03, 04, 06
Byte1: 00, 02, 06
Byte2: 00
Byte3: 00
this is actually subaction flags according to psac
these run all the way to the first subaction main tab entries below


first subaction main tab offset @ 21dfc
last subaction main tab offset @ 225cc (len = 7d0)
first subaction gfx tab offset @ 225d0
last subaction gfx tab offset @ 22da0 (len = 7d0)
first subaction sfx tab offset @ 22da4
last subaction sfx tab offset @ 23574 (len = 7d0)
first subaction other tab offset @ 23578
last subaction other tab offset @ 23d48 (len = 7d0)

looks like a subaction after that, but i don't see where it's called from
offset for this is 23d4c, which would be reffed in-file as 23d4c-80 = 23ccc
interestingly, this is the value that the first variable holds?
strange
string also isn't found anywhere else, just in this one spot

first action entry tab offset @ 1eed0
	-first events params start at 1e438
last action entry tab offset @ 1ef08
first action exit tab offset @ 1ef0c
last action entry tab offset @ 1ef44

beginning of psac offset table @ 24380
that's reffed as 24300, which is found towards the end of the offsets table at @ 32b54


old lambda stuffs
//auto setTargetParam = [&](int targetParam)
					//{
					//	if (paramOffsetNum != SIZE_MAX && targetParam >= 0)
					//	{
					//		targetParamIndexOffset = ((8 * targetParam) + 4);
					//		paramVal = contents.getBytes(4, paramOffsetNum + targetParamIndexOffset, numGotten);
					//		paramValString = numToHexStringWithPadding(hexVecToNum(paramVal), 8);
					//		intermediateParamValString = paramValString;
					//		paramValNum = hexStringToNum(paramValString);
					//		// Get the value of the parameter's type identifier. This ensures that variables are manipulated in the expected way.
					//		paramTypeIdentifier = contents.getBytes(4, paramOffsetNum + targetParamIndexOffset - 4, numGotten);
					//		paramTypeIDNum = lava::hexVecToNum(paramTypeIdentifier);
					//	}
					//};
					//auto saveTargetParam = [&]()
					//{
					//	if (paramOffsetNum != SIZE_MAX)
					//	{
					//		contents.setBytes(numToHexVec(hexStringToNum(paramValString)), paramOffsetNum + targetParamIndexOffset);
					//		contents.setBytes(numToHexVec(paramTypeIDNum), paramOffsetNum + targetParamIndexOffset - 4);
					//	}
					//};
					//setTargetParam(currTarget->paramIndex);